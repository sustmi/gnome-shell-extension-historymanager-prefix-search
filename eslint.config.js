import {defineConfig} from '@eslint/config-helpers';
import gnomeShellExtensionsConfig from 'gnome-shell-extensions-dev-scripts/eslint.config.js';

export default defineConfig([
    gnomeShellExtensionsConfig,
]);
